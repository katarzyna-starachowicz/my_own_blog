.buttons
  - if user_signed_in?
    %button=link_to 'Log out', destroy_user_session_path, method: :delete
    %button=link_to 'Account', edit_user_registration_path
  - else
    %button=link_to 'Sign up', new_user_registration_path
    %button=link_to 'Log in',  new_user_session_path

  - if admin_signed_in?
    %button=link_to 'Create a new post', new_post_path
